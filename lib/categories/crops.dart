import 'package:flutter/material.dart';


class CropsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Crops')),
      body: const Center(child: Text('Welcome to Crops Category')),
    );
  }
}