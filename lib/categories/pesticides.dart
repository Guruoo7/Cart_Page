import 'package:flutter/material.dart';

class PesticidesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pesticides')),
      body: const Center(child: Text('Welcome to Pesticides Category')),
    );
  }
}
